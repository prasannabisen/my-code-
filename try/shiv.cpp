#include<iostream>
#include<string>
using namespace std;
int main()
{
	string s;
	cin >> s;
	cout << s << endl;
	cout << s.length();
	int mid = (s - 1) / 2;
	for (int i = 0; i < (s - 1) / 2; i++, mid++) {
		if (s[i] > s[mid]) {

		}
	}
}